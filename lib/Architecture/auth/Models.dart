class LoggedUser{
  final int id;
  final String displayName;
  final String email;
  final int role;
  final String token;
  LoggedUser( this.id, this.email, this.displayName, this.role, this.token);
}
